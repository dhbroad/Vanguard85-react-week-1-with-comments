import React, { Component } from 'react'

export default class Home extends Component { // every component has a state, but certain variables define that state. Example: someone being signed in is a state.
  render() {
    return (
      <div>Home</div>
    )
  }
}
